engines:
  duplication:
    enabled: true
    config:
      languages:
        - javascript
  eslint:
    enabled: true
  fixme:
    enabled: true

# Exclude test directories
exclude_paths:
- test/**
- performance/**

ratings:
  paths:
    - lib/**
    - "**.js"
