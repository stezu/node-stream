engines:
  duplication:
    enabled: true
    config:
      languages:
        - javascript
  fixme:
    enabled: true

# Exclude test directories
exclude_paths:
- test/**
- performance/**

ratings:
  paths:
    - lib/**
    - "**.js"
